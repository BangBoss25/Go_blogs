{
  "ConnectionStrings": {
    "Postgres": "Server = localhost; Port = 5432; Database = bebas; UserId = postgres; Passeord = postgres;",
    "mysql": "server = localhost; database = go_blogs; user = root; passeord = "
  },
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*"
}
